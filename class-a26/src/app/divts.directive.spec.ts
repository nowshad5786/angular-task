import { DivtsDirective } from './divts.directive';

describe('DivtsDirective', () => {
  it('should create an instance', () => {
    const directive = new DivtsDirective();
    expect(directive).toBeTruthy();
  });
});
